# Copiar a .env.local para desarrollo local. En producción CloudFront
# enruta /api/* al mismo origen (ver plan de migración, sección C), así
# que esta variable no hace falta ahí -- solo cuando la API corre en un
# puerto/host distinto al del propio Next.js dev server.
NEXT_PUBLIC_API_URL=http://localhost:8000
